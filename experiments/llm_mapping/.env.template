# Copy to .env and fill in. .env is gitignored; the key must never be committed.
SECHA_AVIARY_API_KEY=
# Optional: override the endpoint
# SECHA_AVIARY_URL=https://aviary.fgl.rd.tuni.fi/api/chat/completions

# Commercial provider (Mistral). Free-tier keys work.
SECHA_MISTRAL_API_KEY=

# Open-weight frontier arm (NVIDIA NIM). Free tier.
SECHA_NVIDIA_API_KEY=
